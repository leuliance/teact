{
  "name": "@teactjs/cli",
  "version": "0.1.0-alpha.6",
  "description": "CLI tool for the Teact bot framework — create, dev, build, generate",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/src/index.d.ts",
  "bin": {
    "teact": "dist/bin.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "bun build ./src/bin.ts ./src/index.ts --outdir ./dist --format esm --target bun --external vite --external vite-node --external esbuild --external commander --external @clack/prompts && tsc --emitDeclarationOnly --outDir dist --declaration --project tsconfig.build.json"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "commander": "^14.0.3",
    "esbuild": "^0.25.0",
    "vite": "^8.0.3",
    "vite-node": "^6.0.0"
  },
  "license": "MIT"
}
